# Neurocommenting Bot

Telegram-бот для автоматического комментирования постов в каналах и группах с помощью ИИ. Управляется через Telegram-интерфейс, поддерживает несколько рабочих аккаунтов одновременно.

---

## Что умеет бот

### Автокомментирование
- Автоматически комментирует новые посты в указанных каналах и группах
- Генерирует уникальные комментарии через Timeweb AI (ChatGPT-совместимый API)
- Режим **"Комментировать первым"**: сначала занимает место в комментариях быстрым placeholder-сообщением, затем редактирует его готовым текстом от ИИ
- Режим **"Шаблонные комментарии"**: вместо ИИ использует заранее заданные тексты из списка, выбирая случайный

### Управление аккаунтами (воркерами)
- Подключение неограниченного числа Telegram-аккаунтов через сессионные файлы (.session) или по номеру телефона
- Для каждого аккаунта настраивается:
  - **Роль** — системная роль для ИИ (например: "Вы — маркетолог")
  - **Промпт** — задание для ИИ (например: "Напиши короткий комментарий на тему этого поста")
  - **Задержка** — пауза в секундах перед комментарием после выхода поста
  - **Шанс** — вероятность оставить комментарий (1 к N)
  - **Автоответ** — автоматический ответ на входящие личные сообщения
  - **Прокси** — SOCKS5 прокси для аккаунта
  - **Шаблоны коммента** — список текстов для шаблонного режима

### Управление каналами
- Добавление каналов и групп в список для мониторинга (по ссылке или @username)
- Автоматическое вступление в канал/группу при добавлении
- Удаление каналов из списка

### Аутрич в ЛС
- Воркер отслеживает указанные группы/чаты и автоматически пишет в личку всем, кто там пишет
- Настраивается для каждого аккаунта отдельно:
  - **Текст сообщения** — что отправлять в ЛС
  - **Лимит в день** — максимальное количество DM в сутки
  - **Задержка** — пауза перед отправкой DM в секундах
  - **Группы для мониторинга** — список групп, за которыми следит воркер
- Никогда не пишет одному человеку дважды (глобальная дедупликация)
- При достижении дневного лимита останавливается до следующего дня

### Дополнительные функции
- **Инвайтинг** — массовое добавление людей в группу с указанным промежутком
- **Рассылка в личку** — массовая отправка сообщений по списку пользователей
- **Парсинг комментариев** — экспорт комментариев канала в Excel
- **Поиск каналов** — поиск по ключевым словам через Timeweb AI + Telegram Search
- **Редактирование профиля** — смена имени, фамилии, фото, описания, юзернейма
- **Сервисный канал** — все уведомления о новых комментариях и ошибках приходят в указанный канал

---

## Архитектура

```
┌─────────────────────────────────────────────┐
│           Telegram-бот (manager)            │
│   Интерфейс управления через BotFather-бота │
└──────────────────────┬──────────────────────┘
                       │ команды
┌──────────────────────▼──────────────────────┐
│              SQLite база данных             │
│  workers / channels / managers / pretexts  │
└──────────┬──────────────────────────────────┘
           │ настройки
┌──────────▼──────────────────────────────────┐
│        Рабочие аккаунты (workers)           │
│  Реальные Telegram-аккаунты на MTProto      │
│  Слушают каналы/группы и оставляют коменты │
└──────────┬──────────────────────────────────┘
           │ текст поста
┌──────────▼──────────────────────────────────┐
│            Timeweb AI API                   │
│         Генерация текста коммента           │
└─────────────────────────────────────────────┘
```

---

## Требования

- VPS с Ubuntu 22.04 (минимум 1 CPU, 1 GB RAM)
- Python 3.10+
- Аккаунт на [my.telegram.org](https://my.telegram.org) для получения API_ID и API_HASH
- Telegram-бот созданный через [@BotFather](https://t.me/BotFather)
- Аккаунт на [timeweb.cloud](https://timeweb.cloud/services/ai-agents) для доступа к ИИ (раздел AI Agents)

---

## Развёртывание на VDS

### 1. Создай VPS

На [timeweb.cloud](https://timeweb.cloud) → Облачные серверы → Создать:
- ОС: Ubuntu 22.04
- Тариф: минимальный (1 CPU, 1 GB RAM хватает для 5–10 аккаунтов)
- Запомни IP и пароль root

### 2. Подключись по SSH

```bash
ssh root@<IP_СЕРВЕРА>
```

### 3. Установи зависимости

```bash
apt update && apt install python3 python3-pip python3-venv git -y
```

### 4. Склонируй репозиторий

```bash
git clone https://github.com/tw2igor/neurocommenting-bot /root/ComBot
cd /root/ComBot
```

### 5. Создай виртуальное окружение и установи библиотеки

```bash
python3 -m venv venv
source venv/bin/activate
pip install requests xlsxwriter tgcrypto
pip install "git+https://github.com/KurimuzonAkuma/pyrogram/archive/dev.zip"
```

> Используется форк Pyrogram от KurimuzonAkuma — он поддерживает актуальные функции Telegram API.
> `tgcrypto` — опциональная C-библиотека для ускорения MTProto-шифрования. Без неё бот работает, но медленнее.

### 6. Получи Telegram API credentials

Зайди на [my.telegram.org](https://my.telegram.org) → API development tools → создай приложение. Запомни `api_id` и `api_hash`.

### 7. Создай управляющего бота

Напиши [@BotFather](https://t.me/BotFather) → `/newbot` → задай имя и username. Получи токен вида `123456789:AAxxxxx...`.

Чтобы узнать `BOT_ID` (числовой ID бота) — возьми числовую часть токена до двоеточия: `123456789`.

### 8. Получи Timeweb AI credentials

Зарегистрируйся на [timeweb.cloud/services/ai-agents](https://timeweb.cloud/services/ai-agents):
- Создай AI-агента и скопируй **Agent Access ID** (`TIMEWEB_AGENT_ID`)
- В настройках профиля создай **Bearer-токен** (`TIMEWEB_TOKEN`)

### 9. Настрой конфигурацию

Скопируй пример конфига и заполни своими данными:

```bash
cp config.example.py config.py
nano config.py
```

Содержимое `config.py`:

```python
# Telegram App (my.telegram.org)
API_ID = 12345678
API_HASH = 'your_api_hash_here'

# Управляющий бот (@BotFather)
BOT_ID = 123456789
BOT_LINK = 'your_bot_username'        # без @
BOT_TOKEN = '123456789:AAxxxxxxxx...'

# Telegram ID администраторов бота
ADMINS = (123456789,)                 # твой Telegram ID, можно несколько через запятую

# Timeweb AI (agent.timeweb.cloud)
TIMEWEB_TOKEN = 'eyJ...'
TIMEWEB_AGENT_ID = 'xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx'
```

> `config.py` не попадает в git — он в `.gitignore`.

Узнать свой Telegram ID можно через [@userinfobot](https://t.me/userinfobot).

### 10. Настрой автозапуск через systemd

```bash
cp systemd/com.service /etc/systemd/system/
systemctl daemon-reload
systemctl enable com
systemctl start com
```

### 11. Проверь запуск

```bash
systemctl status com
```

Логи в реальном времени:

```bash
journalctl -u com -f
```

---

## Первый запуск

1. Напиши своему боту `/start` в Telegram — должно появиться главное меню
2. Нажми **"Добавить аккаунт"** и следуй инструкции:
   - Введи номер телефона в международном формате (`+79001234567`)
   - Введи код подтверждения из Telegram
   - Если включена двухфакторная аутентификация — введи пароль
3. После добавления аккаунт появится в разделе **"Мои аккаунты"**
4. Зайди в настройки аккаунта и настрой:
   - **Роль** — кем представляется ИИ
   - **Промпт** — что именно писать в комментариях
   - **Задержку** и **шанс** по желанию
5. Нажми **"Добавить каналы 💬"** и отправь ссылки на каналы/группы, каждую с новой строки
6. Бот начнёт автоматически комментировать новые посты

---

## Обновление с GitHub

После изменений в коде обнови сервер:

```bash
cd /root/ComBot && git pull origin master && systemctl restart com
```

---

## Структура файлов

```
ComBot/
├── app.py                  # Основной файл — вся логика бота
├── config.py               # Секретные данные (не в git)
├── config.example.py       # Пример конфига для новой установки
├── handlers.py             # Механизм ожидания ввода от пользователя
├── menus.py                # Меню шаблонов коммента
├── sqlite_functions.py     # Работа с базой данных
├── systemd/
│   └── com.service         # Файл автозапуска для systemd
├── database.db             # База данных SQLite (создаётся автоматически)
└── *.session               # Сессии Pyrogram (создаются при добавлении аккаунтов)
```

---

## Частые вопросы

**Бот запустился, но не комментирует**
- Убедись что добавил каналы через "Добавить каналы 💬" в настройках аккаунта
- Проверь что аккаунт вступил в канал (бот делает это автоматически при добавлении)
- Посмотри логи: `journalctl -u com -f`

**Ошибка при добавлении аккаунта**
- Номер должен быть в международном формате: `+79001234567`

**Как добавить несколько администраторов**
- В `config.py` в поле `ADMINS` перечисли ID через запятую: `ADMINS = (111111111, 222222222)`

**Как настроить прокси для аккаунта**
- В настройках аккаунта есть кнопка прокси
- Формат: `socks5:host:port` или `socks5:host:port:user:pass` для аутентифицированного

**Как настроить глобальный прокси (для всех подключений)**
- В `config.py` задай поле `PROXY`:
  ```python
  PROXY = 'socks5:45.145.57.238:10253'             # анонимный
  PROXY = 'socks5:45.145.57.238:10253:user:pass'   # с авторизацией
  ```
- Глобальный прокси применяется ко всем клиентам — менеджер-боту, воркерам, добавлению аккаунтов
- Если у аккаунта задан свой прокси — он имеет приоритет над глобальным
